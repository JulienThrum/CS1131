import org.junit.Test;

import static org.junit.Assert.*;

public class ThursdayTest {

    @org.junit.Test
    public void abs() {
        Thursday derp = new Thursday();
        if (derp.abs(-1) != 1) {
            fail("Absolute Value Failed abs(-1) != 1");
        }
    }

    @org.junit.Test
    public void abs1() {
        Thursday derp = new Thursday();
        if (derp.abs(-1) != 1) {
            fail("Absolute Value Failed abs(-1) != 1");
        }
    }
}